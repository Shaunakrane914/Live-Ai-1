# AegisGrid AI Engine — Python Dependencies
# Python 3.11+
# Install: pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────
torch>=2.2.0
numpy>=1.26.0

# ── Gymnasium RL environment ──────────────────────────
gymnasium>=0.29.1

# ── FastAPI & WebSocket server ────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.6.0

# ── Utilities ─────────────────────────────────────────
python-dotenv>=1.0.0

# ── Blockchain bridge (graceful no-op if Hardhat offline) ──
web3>=6.15.0

# ── Data preprocessing (aegis_training_data.csv) ─────
pandas>=2.2.0

# ── Node.js gateway socket client (watch_tick.py) ─────
python-socketio[asyncio_client]>=5.0.0

# ── Testing ───────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Type Checking ──────────────────────────────────────
mypy>=1.8.0

# ── Code Formatting ───────────────────────────────────
black>=24.0.0
isort>=5.13.0
