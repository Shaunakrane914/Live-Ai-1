# Force CPU-only PyTorch to prevent massive NVIDIA package downloads
--extra-index-url https://download.pytorch.org/whl/cpu
torch

numpy>=1.26.0

# ── Gymnasium RL environment ──────────────────────────
gymnasium>=0.29.1

# ── FastAPI & WebSocket server ────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.6.0

# ── Utilities ─────────────────────────────────────────
python-dotenv>=1.0.0

# ── Blockchain bridge (graceful no-op if Hardhat offline) ──
web3>=6.15.0

# ── Data preprocessing (aegis_training_data.csv) ─────
pandas>=2.2.0

# ── Node.js gateway socket client (watch_tick.py) ─────
python-socketio[asyncio_client]>=5.0.0
